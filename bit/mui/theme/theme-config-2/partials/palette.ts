import { PaletteOptions } from "@mui/material";

const palette: { palette: PaletteOptions } = {
  palette: {
    mode: "light",
    primary: {
      main: "#007cc0",
    },
    secondary: {
      main: "#e42525",
    },
  },
};

export default palette;