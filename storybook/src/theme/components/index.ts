import { Components, Theme } from "@mui/material";
import { acuraButton, hondaButton } from "./Button";

export const hondaComponents = {
  components: {
    ...hondaButton,
  },
};

export const acuraComponents = {
  components: {
    ...acuraButton,
  },
};
